apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: podinfo
  namespace: podinfo
spec:
  interval: 30m
  chart:
    spec:
      chart: podinfo
      version: "6.9.4"
      sourceRef:
        kind: HelmRepository
        name: podinfo
  values:
    ui:
      color: "#34577c"
      message: "Hello"
    replicaCount: 2
    service:
      enabled: true
      type: ClusterIP