apiVersion: v1
kind: Namespace
metadata:
  name: spatia
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: spatia-tls
  namespace: spatia
spec:
  secretName: spatia-tls-secret
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - spatia.rupan.dev
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: website-deploy
  namespace: spatia
spec:
  replicas: 1
  selector:
    matchLabels:
      app: spatia-web
  template:
    metadata:
      labels:
        app: spatia-web
      annotations:
        reloader.stakater.com/auto: "true"
    spec:
      containers:
        - name: web
          image: jeff7712/spatia-site@sha256:a244e8e830c887ad7c47d340339833aeaa4f335acf2b4bf5c960430a5f282d09
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            runAsUser: 101
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: spatia-route
  namespace: spatia
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`spatia.rupan.dev`) || Host(`www.spatia.rupan.dev`)
      kind: Rule
      services:
        - name: spatia-svc
          port: 80
  tls:
    secretName: spatia-tls-secret
---
apiVersion: v1
kind: Service
metadata:
  name: spatia-svc
  namespace: spatia
spec:
  selector:
    app: spatia-web
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
