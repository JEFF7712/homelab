apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: apptly-tls
spec:
  secretName: apptly-tls-secret
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - apptly.rupan.dev
