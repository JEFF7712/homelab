apiVersion: batch/v1
kind: Job
metadata:
  name: minio-bucket-reconcile
  namespace: observability
  annotations:
    argocd.argoproj.io/hook: Sync
    argocd.argoproj.io/hook-delete-policy: BeforeHookCreation,HookSucceeded
    argocd.argoproj.io/sync-wave: "1"
spec:
  backoffLimit: 6
  template:
    spec:
      restartPolicy: OnFailure
      containers:
        - name: bootstrap
          image: docker.io/minio/mc@sha256:a7fe349ef4bd8521fb8497f55c6042871b2ae640607cf99d9bede5e9bdf11727
          command:
            - /bin/sh
            - -c
          args:
            - |
              set -eu

              i=0
              until mc alias set local http://minio.observability.svc:9000 "$MINIO_ROOT_USER" "$MINIO_ROOT_PASSWORD"; do
                i=$((i + 1))
                if [ "$i" -ge 60 ]; then
                  exit 1
                fi
                sleep 5
              done

              for bucket in \
                loki-data \
                loki-ruler \
                tempo-data \
                mimir-data \
                mimir-ruler \
                mimir-alertmanager
              do
                mc mb --ignore-existing "local/${bucket}"
                mc anonymous set none "local/${bucket}"
              done
          env:
            - name: MINIO_ROOT_USER
              valueFrom:
                secretKeyRef:
                  name: minio-creds
                  key: rootUser
            - name: MINIO_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: minio-creds
                  key: rootPassword
