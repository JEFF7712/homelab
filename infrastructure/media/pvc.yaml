apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media-pvc-hdd
  namespace: media
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: longhorn-media
  resources:
    requests:
      storage: 1500Gi