apiVersion: v1
kind: Service
metadata:
  name: moltbot-manager
  namespace: moltbot
spec:
  selector:
    app: moltbot-manager
  ports:
  - name: http
    port: 18789
    targetPort: 18789
  type: ClusterIP
